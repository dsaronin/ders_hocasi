%h5 Select an entry from #{@settings[:source]}
= form('/source', method=:post) do |f|
  = f.label(:source, 'Source entry:&emsp;')
  = f.select(:source, Module.const_get( @settings[:source] ).sorted_keys )
  = link_to( 'cancel', "/", { :id => "cancel", :title => "cancel setting changes" } )
  = submit( 'update', :value => 'flashcards »' )
 
